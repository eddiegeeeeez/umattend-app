services:
  client:
    build:
      context: ./client
      args:
        NODE_ENV: production
        NEXT_PUBLIC_API_URL: https://umattend.site
    image: umattend-client:prod
    container_name: umattend_client_prod
    restart: unless-stopped
    expose:
      - "3000"
    env_file:
      - ./client/.env.production
    environment:
      PORT: "3000"
      NODE_ENV: production
    networks:
      - umattend_net

  server:
    build:
      context: ./server
    image: umattend-server:prod
    container_name: umattend_server_prod
    restart: unless-stopped
    expose:
      - "4000"
    env_file:
      - ./server/.env.production
    environment:
      PORT: "4000"
    networks:
      - umattend_net

  nginx:
    image: nginx:1.27-alpine
    container_name: umattend_nginx_prod
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/prod.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot_conf:/etc/letsencrypt:ro
      - certbot_www:/var/www/certbot:ro
    depends_on:
      - client
      - server
    networks:
      - umattend_net

  certbot:
    image: certbot/certbot:latest
    container_name: umattend_certbot_prod
    volumes:
      - certbot_conf:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    entrypoint: >
      /bin/sh -c "trap exit TERM;
      while :; do
        certbot renew --webroot --webroot-path=/var/www/certbot --quiet;
        sleep 12h & wait $${!};
      done"

volumes:
  certbot_conf:
  certbot_www:

networks:
  umattend_net:
    driver: bridge
